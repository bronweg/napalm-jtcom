TASK: Final Step 4 cleanup — remove deprecated force_delete_vlan entirely and finish policy layer polish

GOAL

Make the Step 4 policy layer clean, consistent, and production-grade by:

1) Removing the deprecated alias `force_delete_vlan` completely
2) Keeping only the canonical flag name:
   `allow_vlan_delete_in_use`
3) Eliminating the Ansible/action-plugin alias bug at the root by removing alias handling entirely
4) Preserving the already implemented improvements:
   - unified warning structure
   - DEFAULT_FALLBACK_VLAN_ID constant
   - Step 4 policy semantics
5) Verifying that mode-none fallback still does not bypass mode-change protection
6) Verifying delete-in-use behavior remains correct and deterministic

RATIONALE

The module/collection is not yet in external use.
There is no real backward compatibility obligation.
Keeping `force_delete_vlan` now only adds:
- complexity
- ambiguity
- code paths that can break
- avoidable future cleanup

Therefore the old name must be removed completely.

STRICT NON-GOALS

Do NOT:
- redesign Step 4 behavior
- change policy decisions
- reintroduce alias compatibility
- weaken safety checks
- change Step 1 / Step 2 / Step 3 semantics
- redesign warning schema again
- change None vs [] behavior

This task is cleanup and correctness only.

FINAL TARGET STATE

After this task:

- `allow_vlan_delete_in_use` is the only supported flag name
- `force_delete_vlan` does not appear anywhere in code, docs, tests, examples, or action/plugin layers
- any code paths depending on the old alias are removed
- Ansible action/plugin no longer accidentally passes conflicting delete-in-use flags
- Step 4 behavior remains otherwise unchanged

FILES TO MODIFY

Primary:
- src/napalm_jtcom/driver.py
- src/napalm_jtcom/utils/vlan_membership.py

Ansible:
- ansible/action_plugins/jtcom_config.py
- ansible/library/jtcom_config.py
- galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py
- galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py

Docs:
- README.md
- galaxy/bronweg/cgiswitch/README.md
- examples if present

Tests:
- tests/unit/test_vlan_membership_apply.py
- tests/unit/test_port_vlan_input.py
- any tests mentioning force_delete_vlan
- any docs/example tests if present

Full repo grep required.

PART 1 — REMOVE force_delete_vlan ENTIRELY

Search the entire repo for:
- force_delete_vlan

Remove or rename every occurrence.

This includes:
- driver optional_args parsing
- __init__ handling
- apply_device_config / set_vlans parameter parsing
- action plugins
- module docs
- Galaxy module docs
- README
- Galaxy README
- examples
- tests
- comments
- warnings / hints
- staged task files only if they are intentionally part of repo and should reflect final naming

Required final rule:
- only `allow_vlan_delete_in_use` exists
- no alias support
- no deprecation path
- no special error about “both names provided”
- no hidden support for old flag name

Acceptance requirement:
A repo-wide grep for `force_delete_vlan` must return nothing.

PART 2 — FIX ANSIBLE / ACTION PLUGIN FLAG PASS-THROUGH

Files:
- ansible/action_plugins/jtcom_config.py
- galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py

Current bug source:
- new flag was always passed with a default False value
- alias handling created ambiguous state

Now that alias is removed, the action plugins must be simplified.

Required behavior:
- only pass `allow_vlan_delete_in_use` if it is actually present in user input
- do not inject default False into optional_args unless the project consistently expects explicit booleans there
- preserve existing behavior for other flags exactly as intended

Preferred implementation:
if "allow_vlan_delete_in_use" in p:
    optional_args["allow_vlan_delete_in_use"] = p.get("allow_vlan_delete_in_use")

Important:
Use the same pattern as semantically optional flags elsewhere.

Required tests:
A) allow_vlan_delete_in_use provided -> passed through
B) allow_vlan_delete_in_use absent -> not injected spuriously
C) no traces of old alias behavior remain

PART 3 — CLEAN DRIVER FLAG HANDLING

File:
- src/napalm_jtcom/driver.py

Required changes:
1) Remove any alias parsing logic
2) Remove any “both flags supplied” validation
3) Keep only one internal policy flag:
   - allow_vlan_delete_in_use
4) Ensure optional_args handling is clean and deterministic

The driver should:
- read allow_vlan_delete_in_use if present
- otherwise default to False
- not know anything about force_delete_vlan anymore

Acceptance:
- driver code becomes simpler
- no dead compatibility code remains

PART 4 — VERIFY MODE-NONE FALLBACK VS MODE-CHANGE PROTECTION

This was previously identified as a subtle correctness risk.

You must explicitly verify and, if needed, correct the planning order so that:

1) canonical desired state is built
2) mode-none fallback maps empty membership to access VLAN 1
3) the resulting effective desired state is then still checked against access↔trunk mode-change policy

Required invariant:
Fallback to VLAN 1 must NOT create a hidden trunk→access transition that bypasses `allow_port_mode_change`.

Required implementation check:
- inspect planning flow in src/napalm_jtcom/utils/vlan_membership.py
- confirm whether mode-change detection runs after fallback
- if not, fix the order

Required tests:
A) current trunk with only tagged VLAN 20, no native
   desired removes tagged 20
   effective fallback => access VLAN 1
   default apply must fail unless allow_port_mode_change=True

B) same in check mode
   warning only

C) same with allow_port_mode_change=True
   success

D) current access port losing last untagged VLAN
   fallback => access VLAN 1
   should NOT require allow_port_mode_change

This part is not about changing Step 4 semantics.
It is about making sure the existing semantics are enforced correctly.

PART 5 — VERIFY DELETE-IN-USE + SET-STYLE INTERACTION

This was the second important correctness concern.

You must verify and, if needed, fix the implementation so that:
- allow_vlan_delete_in_use=True
- VLAN marked absent
- current state uses that VLAN on some ports
- effective auto-detach does NOT create invalid mixed set/remove config semantics

Required rule:
The delete-in-use policy must not create VlanConfig objects that violate the model’s set-vs-remove rules.

Preferred implementation:
- operate through effective detach planning
- do not mutate user-provided VlanConfig into an invalid mixed object

Required tests:
A) delete-in-use with current tagged members and allow_vlan_delete_in_use=True
B) delete-in-use with current untagged member and allow_vlan_delete_in_use=True
C) delete-in-use with set-style membership context
D) dual syntax conflict still fails normally

This is mostly a verification/cleanup task, not a redesign.

PART 6 — KEEP AND VERIFY UNIFIED WARNING STRUCTURE

Do not redesign again, but ensure consistency remains.

Warnings should continue to use the common structure:
- type
- entity
- message
- port_id / vlan_id where applicable
- hint where applicable

Required work:
- scan all policy warnings
- ensure none of them use old ad hoc shape
- ensure docs/examples do not describe stale warning format

Required tests:
A) untagged_move warning contains required fields
B) vlan_delete_in_use warning contains required fields
C) mode_none_mapped_to_vlan1 warning contains required fields
D) mode-change warning shape remains acceptable/consistent enough

PART 7 — KEEP AND VERIFY DEDUP + DEFAULT CONSTANT

Do not redesign, just verify and clean if needed.

A) DEFAULT_FALLBACK_VLAN_ID
- ensure all raw uses of literal VLAN 1 in policy fallback paths are replaced with the constant where appropriate
- do not replace legitimate unrelated VLAN 1 mentions in tests/docs unless they are specifically about the fallback policy

B) Dedup lists
- ensure any policy-constructed port lists are deduplicated and sorted
- especially:
  - affected_ports_tagged
  - affected_ports_untagged
  - effective remove targets
  - any warning lists

Required tests:
- repeated membership references do not create duplicated warning port lists
- effective policy-generated remove operations are unique

PART 8 — DOCUMENTATION CLEANUP

Update all docs so the final Step 4 policy is described accurately.

Required wording/topics:
1) Untagged/native move:
   - fail by default
   - use `allow_untagged_move=true`

2) Delete VLAN in use:
   - fail by default
   - use `allow_vlan_delete_in_use=true`

3) Mode-none policy:
   - ports that would otherwise have no VLAN membership are mapped to access VLAN 1
   - this fallback can still trigger access↔trunk protection if the effective result changes port mode

4) Remove all mention of:
   - force_delete_vlan
   - deprecated alias
   - dual-name support

Required files:
- README.md
- galaxy/bronweg/cgiswitch/README.md
- ansible/library/jtcom_config.py docs
- galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py docs
- examples if needed

Acceptance:
- final user-facing docs only mention allow_vlan_delete_in_use

PART 9 — TEST CLEANUP

Update or remove tests related to deprecated alias support.

Delete tests that assert:
- old alias works
- both names conflict

Replace them with tests that assert:
- only allow_vlan_delete_in_use is supported
- action plugin passes it correctly
- no old alias remains

Keep/add tests for:
- new flag behavior
- mode-change interaction after fallback
- delete-in-use safety
- warning schema
- dedup behavior if not already covered

PART 10 — REPO-WIDE CONSISTENCY SWEEP

After implementation, do a final grep/search for:
- force_delete_vlan
- "both flags"
- any deprecation text related to old alias

And separately verify:
- allow_vlan_delete_in_use appears consistently in code/docs/tests

Acceptance:
- grep for force_delete_vlan returns no results anywhere in tracked files

PART 11 — QUALITY GATES

Run:
1) targeted pytest for all touched policy tests
2) full pytest if feasible
3) ruff on changed Python files
4) mypy on changed src files
5) git diff --check

PART 12 — DELIVERABLE FORMAT

When finished, report back with:

1) Summary of final Step 4 cleanup
2) Modified files
3) Confirmation that force_delete_vlan has been removed completely
4) Confirmation that allow_vlan_delete_in_use is the only supported flag name
5) Confirmation that mode-none fallback still respects allow_port_mode_change
6) Confirmation that delete-in-use policy remains valid with set-style semantics
7) Test results
8) Explicit statement:
   "Step 4 final cleanup is ready for review"

