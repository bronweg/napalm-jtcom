STEP 1 — FINAL FIXUP
Preserve the semantic distinction between “field omitted” (None) and “explicit empty replacement” ([]),
and clean up the remaining Step 1 issues.

CONTEXT

The latest implementation correctly changed:
- tagged_ports / untagged_ports to Optional[list[int]]
- normalized_membership() to use `is not None`

However, there is still a semantic bug in:
- src/napalm_jtcom/utils/normalize.py

Current code uses patterns like:
- cfg.tagged_ports or []
- cfg.untagged_ports or []

This destroys the distinction between:
- None  → field omitted / unspecified
- []    → explicit empty replacement

That distinction is central to the new model and must be preserved everywhere.

GOAL

Fix the remaining semantic leak in normalize-layer and clean up a few small issues in `vlan.py`
before Step 1 is considered truly complete.

STRICT NON-GOALS

Do NOT:
- change driver behavior
- change device logic
- change action plugin behavior
- implement Step 2 logic
- change runtime switch behavior

This is still a pure model/normalization fix.

FILES TO MODIFY

Primary:
- src/napalm_jtcom/utils/normalize.py
- src/napalm_jtcom/model/vlan.py

Tests:
- add or update tests covering normalize-layer behavior
  (put them where it best fits current repo structure)

REQUIRED CHANGES

==================================================
1) FIX normalize_vlan_config() TO PRESERVE None
==================================================

File:
- src/napalm_jtcom/utils/normalize.py

Current buggy behavior:
- cfg.tagged_ports or [] turns None into []
- cfg.untagged_ports or [] turns None into []

This is not allowed.

Required behavior:
- if cfg.tagged_ports is None → normalized tagged_ports must remain None
- if cfg.tagged_ports == []   → normalized tagged_ports must remain []
- same for untagged_ports

Implement it explicitly, for example:

- tagged = None if cfg.tagged_ports is None else sorted(set(cfg.tagged_ports))
- untagged = None if cfg.untagged_ports is None else sorted(set(cfg.untagged_ports))

Then perform the existing “prefer untagged over tagged” cleanup only where appropriate.

IMPORTANT:
If either side is None, do not silently coerce it into [].

Be careful with overlap-removal logic:
- if tagged is None, leave it None
- if untagged is None, leave it None
- only apply list/set overlap cleanup to actual lists

Expected examples:
- tagged_ports=None, untagged_ports=None
  -> result keeps both as None

- tagged_ports=[], untagged_ports=[]
  -> result keeps both as []

- tagged_ports=[1,2], untagged_ports=[2,3]
  -> result tagged_ports=[1], untagged_ports=[2,3]

==================================================
2) REMOVE UNUSED VARIABLE tagged_ops
==================================================

File:
- src/napalm_jtcom/model/vlan.py

There is an unused local variable:
- tagged_ops = [...]

Remove it.

==================================================
3) REMOVE REDUNDANT TAGGED CONFLICT CHECK
==================================================

File:
- src/napalm_jtcom/model/vlan.py

There are currently two checks around tagged_set conflicts.
One of them is redundant / unnecessarily complex.

Keep only the simple, explicit rule:

- if self.tagged_set is not None and (self.tagged_add is not None or self.tagged_remove is not None):
    raise ValueError(...)

Delete the extra redundant check.

Do the same level of clarity for untagged if needed:
- keep the simple explicit presence-based check
- avoid clever or indirect logic

==================================================
4) PEP 8 / SMALL STYLE CLEANUP
==================================================

File:
- src/napalm_jtcom/model/vlan.py

Ensure:
- proper blank line spacing between top-level helper functions
- imports are clean
- formatting is consistent

No unrelated refactors.

==================================================
5) ADD TESTS FOR normalize_vlan_config() SEMANTICS
==================================================

Add tests that explicitly verify the normalize-layer preserves None vs [].

Test cases required:

A) None stays None
- input:
    VlanConfig(vlan_id=20, tagged_ports=None, untagged_ports=None)
- after normalize_vlan_config():
    tagged_ports is None
    untagged_ports is None

B) Empty list stays empty list
- input:
    VlanConfig(vlan_id=20, tagged_ports=[], untagged_ports=[])
- after normalize_vlan_config():
    tagged_ports == []
    untagged_ports == []

C) Deduplication still works for explicit lists
- input:
    VlanConfig(vlan_id=20, tagged_ports=[1,1,2], untagged_ports=[3,3])
- after normalize_vlan_config():
    tagged_ports == [1,2]
    untagged_ports == [3]

D) Untagged still wins on overlap, but only for actual lists
- input:
    VlanConfig(vlan_id=20, tagged_ports=[1,2], untagged_ports=[2,3])
- after normalize_vlan_config():
    tagged_ports == [1]
    untagged_ports == [2,3]

E) If tagged is None and untagged has values, tagged remains None
- input:
    VlanConfig(vlan_id=20, tagged_ports=None, untagged_ports=[2,3])
- after normalize_vlan_config():
    tagged_ports is None
    untagged_ports == [2,3]

F) If untagged is None and tagged has values, untagged remains None
- input:
    VlanConfig(vlan_id=20, tagged_ports=[1,2], untagged_ports=None)
- after normalize_vlan_config():
    tagged_ports == [1,2]
    untagged_ports is None

Put these tests into the most appropriate test file for normalize helpers/config normalization.

==================================================
6) VERIFY Step 1 CONTRACT STILL HOLDS
==================================================

After the fix, confirm all of the following remain true:

- legacy tagged_ports=[] still means explicit replacement with empty set
- legacy tagged_ports=None still means field omitted / unspecified
- same for untagged_ports
- normalized_membership() semantics unchanged
- no driver/runtime behavior changed

==================================================
DELIVERABLE FORMAT
==================================================

When finished, report back with:

1) Short summary of what was fixed
2) List of modified files
3) Confirmation that None vs [] is now preserved end-to-end in model + normalize-layer
4) Test results
5) Confirmation that no driver/runtime behavior changed

