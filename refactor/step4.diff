diff --git a/README.md b/README.md
index 9745d90..5569572 100644
--- a/README.md
+++ b/README.md
@@ -65,6 +65,18 @@ list are affected. Unlisted items are always left untouched.
 - VLAN 1 is protected and can never be deleted.
 - Port 6 (management uplink) can never be administratively disabled.
 
+### VLAN Membership Policy
+
+Potentially destructive or ambiguous VLAN membership changes are policy-gated:
+
+- Untagged/native VLAN moves fail by default. Set `allow_untagged_move=True`
+  only when moving a port from one untagged/native VLAN to another is intended.
+- Deleting a VLAN that is still tagged or untagged on any port fails by default.
+  Set `allow_vlan_delete_in_use=True` to auto-detach affected ports before deletion.
+- If a changed port would otherwise end up with no VLAN membership, the policy
+  layer maps it explicitly to access VLAN 1 and emits a structured
+  `mode_none_mapped_to_vlan1` warning.
+
 ---
 
 ## Python API
diff --git a/ansible/action_plugins/jtcom_config.py b/ansible/action_plugins/jtcom_config.py
index 6422396..c407b52 100644
--- a/ansible/action_plugins/jtcom_config.py
+++ b/ansible/action_plugins/jtcom_config.py
@@ -98,6 +98,8 @@ class ActionModule(ActionBase):  # type: ignore[misc]
             "backup_before_change": p.get("backup_before_change", True),
             "safety_port_id": p.get("safety_port_id", 6),
             "allow_port_mode_change": p.get("allow_port_mode_change", False),
+            "allow_untagged_move": p.get("allow_untagged_move", False),
+            "allow_vlan_delete_in_use": p.get("allow_vlan_delete_in_use", False),
         }
         if p.get("port") is not None:
             optional_args["port"] = p["port"]
diff --git a/ansible/library/jtcom_config.py b/ansible/library/jtcom_config.py
index b707402..072c1a9 100644
--- a/ansible/library/jtcom_config.py
+++ b/ansible/library/jtcom_config.py
@@ -53,6 +53,18 @@ options:
     description: Allow VLAN membership operations to change a port between access and trunk mode.
     type: bool
     default: false
+  allow_untagged_move:
+    description: >
+      Allow destructive untagged/native VLAN moves. By default, changing a port
+      from one untagged/native VLAN to another fails in apply mode.
+    type: bool
+    default: false
+  allow_vlan_delete_in_use:
+    description: >
+      Allow deleting VLANs still referenced by ports by auto-detaching those
+      ports first. This is a destructive override.
+    type: bool
+    default: false
   vlans:
     description: >
       Incremental VLAN changes, keyed by VLAN ID (string or int).
@@ -119,6 +131,17 @@ EXAMPLES = r"""
       8:
         native_vlan: 10
         trunk_set_vlans: [20, 30]
+    allow_untagged_move: true
+
+- name: Force-delete a VLAN after detaching it from ports
+  jtcom_config:
+    host: 192.0.2.1
+    username: "{{ jtcom_user }}"
+    password: "{{ jtcom_pass }}"
+    vlans:
+      20:
+        state: absent
+    allow_vlan_delete_in_use: true
 """
 
 RETURN = r"""
@@ -161,6 +184,8 @@ def main() -> None:
             backup_before_change=dict(type="bool", default=True),
             safety_port_id=dict(type="int", default=6),
             allow_port_mode_change=dict(type="bool", default=False),
+            allow_untagged_move=dict(type="bool", default=False),
+            allow_vlan_delete_in_use=dict(type="bool", default=False),
             vlans=dict(type="dict"),
             ports=dict(type="dict"),
         ),
diff --git a/galaxy/bronweg/cgiswitch/README.md b/galaxy/bronweg/cgiswitch/README.md
index 1d62dbe..79135f7 100644
--- a/galaxy/bronweg/cgiswitch/README.md
+++ b/galaxy/bronweg/cgiswitch/README.md
@@ -29,6 +29,15 @@ Idempotent, diff-aware PATCH-style configuration of VLANs and ports.
 - Port 6 (management uplink) cannot be administratively disabled
 - VLAN 1 cannot be deleted
 
+VLAN membership policy:
+
+- Untagged/native VLAN moves fail by default; use `allow_untagged_move: true`
+  only when the move is intended.
+- Deleting a VLAN still used by ports fails by default; use
+  `allow_vlan_delete_in_use: true` to auto-detach affected ports before deletion.
+- If a changed port would otherwise have no VLAN membership, it is mapped to
+  access VLAN 1 and a `mode_none_mapped_to_vlan1` warning is returned.
+
 ```yaml
 - name: Configure switch
   bronweg.cgiswitch.jtcom_config:
diff --git a/galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py b/galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py
index 1401fa6..08c3bf6 100644
--- a/galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py
+++ b/galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py
@@ -80,6 +80,8 @@ class ActionModule(ActionBase):  # type: ignore[misc]
             "backup_before_change": p.get("backup_before_change", True),
             "safety_port_id": 6,
             "allow_port_mode_change": p.get("allow_port_mode_change", False),
+            "allow_untagged_move": p.get("allow_untagged_move", False),
+            "allow_vlan_delete_in_use": p.get("allow_vlan_delete_in_use", False),
         }
 
         try:
diff --git a/galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py b/galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py
index 250d8da..8408b7b 100644
--- a/galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py
+++ b/galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py
@@ -43,6 +43,18 @@ options:
     description: Allow VLAN membership operations to change a port between access and trunk mode.
     type: bool
     default: false
+  allow_untagged_move:
+    description: >
+      Allow destructive untagged/native VLAN moves. By default, changing a port
+      from one untagged/native VLAN to another fails in apply mode.
+    type: bool
+    default: false
+  allow_vlan_delete_in_use:
+    description: >
+      Allow deleting VLANs still referenced by ports by auto-detaching those
+      ports first. This is a destructive override.
+    type: bool
+    default: false
   vlans:
     description: >
       Incremental VLAN changes, keyed by VLAN ID (string or int).
@@ -113,6 +125,18 @@ EXAMPLES = r"""
       8:
         native_vlan: 10
         trunk_set_vlans: [20, 30]
+    allow_untagged_move: true
+
+- name: Force-delete a VLAN after detaching it from ports
+  bronweg.cgiswitch.jtcom_config:
+    host: 192.0.2.1
+    username: "{{ jtcom_user }}"
+    password: "{{ jtcom_pass }}"
+    verify_tls: false
+    vlans:
+      20:
+        state: absent
+    allow_vlan_delete_in_use: true
 """
 
 RETURN = r"""
@@ -153,6 +177,8 @@ def main() -> None:
             verify_tls=dict(type="bool", default=True),
             backup_before_change=dict(type="bool", default=True),
             allow_port_mode_change=dict(type="bool", default=False),
+            allow_untagged_move=dict(type="bool", default=False),
+            allow_vlan_delete_in_use=dict(type="bool", default=False),
             vlans=dict(type="dict"),
             ports=dict(type="dict"),
         ),
diff --git a/refactor/step4.txt b/refactor/step4.txt
new file mode 100644
index 0000000..bb1ff81
--- /dev/null
+++ b/refactor/step4.txt
@@ -0,0 +1,554 @@
+STEP 4 — Introduce explicit policy layer for destructive and ambiguous VLAN membership operations
+
+GOAL
+
+Implement a policy layer on top of the existing canonical VLAN membership system.
+
+This step must formalize behavior for:
+- untagged/native VLAN moves
+- deleting VLANs that are still in use
+- resulting port mode == none
+- explicit detach requirements before destructive changes
+- unknown baseline behavior (already warning-based)
+- preservation of strict dual syntax conflict handling
+
+This step is NOT about redesigning the membership engine.
+It is about making destructive and ambiguous operations explicit, safe, and policy-driven.
+
+SELECTED POLICY DECISIONS
+
+These decisions are fixed for this step and must be implemented exactly as follows:
+
+1) Untagged/native move:
+   - default: FAIL
+   - override flag: allow_untagged_move=True
+
+2) Delete VLAN that is still in use:
+   - default: FAIL
+   - override flag: allow_vlan_delete_in_use=True
+
+3) Resulting mode == none:
+   - automatically map to VLAN 1
+   - no allow_mode_none flag
+   - mapping must be explicit in diff / warnings / docs
+
+4) Explicit detach:
+   - required by default for destructive operations
+   - can be bypassed only with explicit override flags
+
+5) Unknown baseline:
+   - keep current behavior
+   - warning only, not fail
+
+6) Dual syntax conflicts:
+   - remain hard FAIL
+   - no silent merge and no precedence rules
+
+STRICT NON-GOALS
+
+Do NOT:
+- redesign Step 1 canonical model
+- redesign Step 2 membership engine
+- redesign Step 3 dual syntax merge model
+- add new input syntaxes
+- weaken existing access↔trunk safety rules
+- change None vs [] semantics
+
+This step must extend policy, not replace architecture.
+
+FILES TO MODIFY
+
+Primary:
+- src/napalm_jtcom/driver.py
+- src/napalm_jtcom/utils/vlan_membership.py
+- src/napalm_jtcom/utils/port_vlan_input.py
+- src/napalm_jtcom/utils/device_diff.py
+- src/napalm_jtcom/model/config.py
+- possibly src/napalm_jtcom/model/vlan.py if policy metadata needs a typed home
+- possibly src/napalm_jtcom/model/port.py if docstrings/examples need policy notes
+
+Ansible:
+- ansible/action_plugins/jtcom_config.py
+- ansible/library/jtcom_config.py
+- galaxy/bronweg/cgiswitch/plugins/action/jtcom_config.py
+- galaxy/bronweg/cgiswitch/plugins/modules/jtcom_config.py
+
+Docs:
+- README.md
+- galaxy/bronweg/cgiswitch/README.md
+- examples if needed
+
+Tests:
+- tests/unit/test_vlan_membership_apply.py
+- tests/unit/test_port_vlan_input.py
+- tests/unit/test_device_plan.py
+- tests/unit/test_vlan_diff.py
+- add new tests where needed
+
+NEW POLICY FLAGS
+
+Add the following explicit flags to the main apply/config path:
+
+- allow_untagged_move: bool = False
+- allow_vlan_delete_in_use: bool = False
+
+Do NOT add allow_mode_none.
+Mode none is handled by automatic VLAN 1 fallback in this step.
+
+These flags must be supported:
+- in driver optional_args or equivalent runtime config
+- in apply_device_config / set_vlans call path
+- in Ansible module and action plugin
+- in Galaxy copies
+- in docs/examples
+
+Important:
+These flags are policy flags and must be clearly documented as potentially dangerous overrides.
+
+HIGH-LEVEL BEHAVIOR
+
+After this step, the flow becomes:
+
+1) Parse and normalize user input
+2) Merge dual syntax into canonical VLAN membership
+3) Read current state
+4) Build canonical per-port membership
+5) Run Step 4 policy transformation / validation
+6) If validation passes:
+   - continue with existing Step 2 membership engine
+   - apply
+   - verify
+7) If validation fails:
+   - fail before device mutation
+
+Policy transformation / validation must happen BEFORE:
+- backup
+- vlan_create
+- vlan_delete
+- vlan_set_port
+- apply_port_changes
+
+PART 1 — UNTAGGED / NATIVE MOVE POLICY
+
+CONTEXT
+
+Currently, assigning a new untagged VLAN to a port overwrites the previous one implicitly.
+
+This is no longer acceptable as default behavior.
+
+Required new behavior:
+- default: fail
+- only allowed when allow_untagged_move=True
+
+Definition of untagged move:
+A port currently has:
+- untagged_vlan = A
+and desired result would make:
+- untagged_vlan = B
+where:
+- A is not None
+- B is not None
+- A != B
+
+This includes both:
+- VLAN-centric input
+- Port-centric input
+- native VLAN changes on trunk ports
+
+Required implementation:
+1) Add a detection helper, e.g.:
+   detect_untagged_move_warnings(current_per_port, desired_per_port) -> list[dict]
+
+Each warning/error entry should contain:
+- type: "untagged_move"
+- port_id
+- current_untagged_vlan
+- desired_untagged_vlan
+- hint mentioning allow_untagged_move=True
+
+2) Planning behavior:
+- if untagged moves are detected and allow_untagged_move is False:
+    - check mode: include warning, do not fail
+    - apply mode: raise dedicated exception before mutation
+
+Recommended exception:
+- VlanMembershipUntaggedMoveError(ValueError)
+
+Message should clearly say:
+- port_id
+- current VLAN
+- desired VLAN
+- override hint
+
+3) If allow_untagged_move=True:
+- allow the move
+- still include warning metadata in check mode and optionally apply result output
+
+Important:
+This is separate from access↔trunk mode-change logic.
+A pure untagged move from VLAN 20 to VLAN 30 on an access port is NOT the same as access→trunk.
+
+Required tests:
+A) port 3 untagged 20 -> desired untagged 30
+   - default apply: fail
+B) same in check mode:
+   - warning only
+C) same with allow_untagged_move=True:
+   - success
+D) same behavior through VLAN-centric and port-centric syntax
+E) same behavior for native_vlan change on trunk port
+
+PART 2 — DELETE VLAN IN USE POLICY
+
+CONTEXT
+
+Deleting a VLAN that is still used by ports must no longer be ambiguous.
+
+Selected policy:
+- default: fail
+- override flag: allow_vlan_delete_in_use=True
+
+Definition of "VLAN in use":
+A VLAN is considered in use if any port currently or finally references it as:
+- untagged/native VLAN
+- tagged VLAN
+
+Required behavior:
+1) If a VLAN is marked:
+   state: absent
+and it is still in use,
+then:
+- default apply: fail
+- check mode: warning
+- apply with allow_vlan_delete_in_use=True: allowed
+
+2) What does allow_vlan_delete_in_use=True mean in this step?
+It means:
+- the system is allowed to detach this VLAN from all affected ports automatically
+- then delete it
+
+This auto-detach must be explicit and deterministic.
+
+IMPORTANT:
+Do NOT silently auto-detach without the flag.
+
+Required implementation:
+A) Add policy planner helper, e.g.:
+   detect_vlan_delete_in_use(current_per_port, desired_vlans, desired_per_port) -> list[dict]
+
+Each warning/error entry should include:
+- type: "vlan_delete_in_use"
+- vlan_id
+- affected_ports_tagged
+- affected_ports_untagged
+- hint mentioning allow_vlan_delete_in_use=True
+
+B) In default apply mode:
+- if any VLAN delete-in-use is detected and allow_vlan_delete_in_use=False:
+    raise dedicated exception before mutation
+
+Recommended exception:
+- VlanDeleteInUseError(ValueError)
+
+C) In check mode:
+- warning only
+
+D) If allow_vlan_delete_in_use=True:
+- auto-detach this VLAN from all affected ports in the desired state before apply
+- then allow normal deletion to continue
+
+Important:
+This means the policy layer must modify the effective desired membership plan when allow_vlan_delete_in_use=True.
+
+Recommended implementation approach:
+- before passing desired input into membership planning,
+  augment the canonical desired_vlans for each absent VLAN:
+    - tagged_remove on all affected ports
+    - untagged_remove on all affected ports
+- then continue through the normal membership engine
+
+This is safer than implementing a special deletion path.
+
+Required tests:
+A) VLAN 20 absent, currently untagged on port 3
+   - default apply: fail
+B) same in check mode:
+   - warning
+C) same with allow_vlan_delete_in_use=True:
+   - auto detach and delete succeeds
+D) VLAN 20 absent, currently tagged on ports 5 and 6
+   - default fail
+E) same with allow_vlan_delete_in_use=True:
+   - tagged membership removed and delete proceeds
+F) mixed case:
+   - native on one port, tagged on another
+G) VLAN absent and already unused:
+   - allowed without warnings
+
+PART 3 — MODE == NONE POLICY
+
+CONTEXT
+
+A desired result may leave a port with:
+- no untagged VLAN
+- no tagged VLANs
+
+This is "mode == none".
+
+Selected policy:
+- automatically map resulting mode == none to access VLAN 1
+- no separate allow flag
+- this remapping must be visible in planning output and documentation
+
+Interpretation:
+- If the canonical desired membership for a port results in no membership at all,
+  the policy layer must transform that port into:
+    access VLAN 1
+before the backend apply stage.
+
+This is a policy decision, not a backend limitation workaround.
+
+REQUIRED BEHAVIOR
+
+1) Detect ports whose desired final mode is "none"
+2) For each such port, transform desired state into:
+   - untagged_vlan = 1
+   - tagged_vlans = empty set
+   - resulting mode = "access"
+3) This transformation must happen before low-level apply
+4) The transformation must be surfaced to the user in diff / warnings / result metadata
+
+Required warning entry:
+- type: "mode_none_mapped_to_vlan1"
+- port_id
+- mapped_vlan: 1
+- reason: "policy_default_vlan1_fallback"
+
+Important:
+This must happen consistently for both:
+- ports that become none via VLAN-centric input
+- ports that become none via port-centric input
+
+Examples:
+A) access VLAN 20, then untagged_remove 20 -> becomes access VLAN 1
+B) trunk with only tagged VLAN 20 and no native, then tagged_remove 20 -> becomes access VLAN 1
+
+DIFF REQUIREMENTS
+
+If fallback to VLAN 1 happens, the diff must not pretend the result is "none".
+It must show the actual effective desired result:
+- untagged_vlan: 1
+- tagged_vlans: []
+
+Additionally, a warning / metadata note must explain that this came from the Step 4 fallback policy.
+
+IMPLEMENTATION REQUIREMENTS
+
+A) Add helper, e.g.:
+   apply_mode_none_fallback(desired_per_port) -> tuple[desired_per_port, warnings]
+
+Behavior:
+- scan desired_per_port
+- for every port classified as "none":
+    set it to access VLAN 1
+- return warnings describing which ports were modified by policy
+
+B) This helper must run before:
+- backend capability checks
+- final desired_port_vlan construction
+or must rebuild desired_port_vlan after transformation
+
+C) This fallback must not bypass access↔trunk mode-change protection.
+If the fallback causes trunk→access, existing mode-change policy still applies.
+
+This is important:
+- mode-none fallback gives the port a valid membership
+- but if that implies a dangerous trunk→access transition, existing allow_port_mode_change rules remain in force
+
+Required tests:
+A) removing last untagged VLAN on access port
+   - desired none -> effective access VLAN 1
+B) removing last tagged VLAN on trunk with no native
+   - desired none -> effective access VLAN 1
+C) diff shows VLAN 1 as final state, not none
+D) warning metadata is present
+E) if fallback results in trunk→access, mode-change protection still behaves exactly as before
+F) no backend unsupported-mode error should occur for these cases anymore, because effective mode is no longer none
+
+PART 4 — EXPLICIT DETACH BY DEFAULT
+
+This is not a separate flag; it is the principle behind the selected policy.
+
+It must be reflected consistently:
+
+- untagged move requires allow_untagged_move
+- delete VLAN in use requires allow_vlan_delete_in_use
+- mode none is not allowed to remain empty; it is mapped explicitly to VLAN 1
+
+There must be no silent destructive operation in default apply mode except the explicitly chosen VLAN 1 fallback policy.
+
+Review all destructive paths and ensure they align with this principle.
+
+PART 5 — UNKNOWN BASELINE POLICY
+
+Keep current behavior:
+- unknown baseline is allowed
+- warning/note/meta only
+- no fail
+
+Required work:
+- ensure Step 4 changes do not accidentally upgrade unknown-baseline warnings into failures
+- preserve structured metadata in diffs
+
+Required tests:
+- existing unknown baseline tests must continue passing
+- no regression in diff shape
+
+PART 6 — DUAL SYNTAX CONFLICTS
+
+Keep current behavior:
+- hard fail
+- no silent resolution
+
+Required work:
+- ensure new Step 4 flags do NOT override or weaken dual syntax conflict detection
+- destructive override flags must not be used as conflict-resolution flags
+
+Example:
+allow_vlan_delete_in_use=True must not allow:
+- VLAN absent
+- port-centric trunk_add_vlans for same VLAN
+
+That remains a hard conflict.
+
+Required tests:
+- existing dual syntax conflict tests continue passing
+- add at least one test proving allow_vlan_delete_in_use does not suppress dual syntax conflict
+- add at least one test proving allow_untagged_move does not suppress dual syntax conflict
+
+PART 7 — IMPLEMENTATION STRATEGY
+
+Recommended order:
+
+1) Add new flags to module/action plugin/Galaxy copies
+2) Add policy exceptions and warning types
+3) Extend membership planning helper(s):
+   - detect untagged move
+   - detect delete-in-use
+   - detect resulting mode == none before fallback
+4) Inject policy validation / transformation into driver before mutation
+5) For allow_vlan_delete_in_use=True:
+   - augment canonical desired_vlans with detach operations before planning/apply
+6) For mode == none:
+   - apply VLAN 1 fallback to desired_per_port before final intent/apply
+7) Keep Step 2 engine intact as much as possible
+8) Update docs/examples
+9) Update tests
+
+PART 8 — ANSIBLE / USER-FACING API
+
+Update Ansible docs and module schema to support:
+- allow_untagged_move
+- allow_vlan_delete_in_use
+
+Do NOT add allow_mode_none.
+
+Descriptions must be explicit and warn users that these flags allow destructive operations.
+
+Examples to add:
+1) explicit untagged move
+2) forced delete of VLAN still in use
+3) mode-none fallback to VLAN 1 example
+
+PART 9 — DIFF / WARNING OUTPUT
+
+Extend result output so policy warnings appear consistently in:
+- check mode
+- possibly apply result metadata when override flag is used
+- fallback cases
+
+Use structured warning objects.
+
+Recommended warning types:
+- "untagged_move"
+- "vlan_delete_in_use"
+- "mode_none_mapped_to_vlan1"
+- existing mode-change warning type can remain or be normalized later
+
+Keep output machine-readable.
+
+PART 10 — TEST MATRIX
+
+At minimum add/update tests covering:
+
+UNTAGGED MOVE
+- default fail
+- check warning
+- override success
+- VLAN-centric path
+- port-centric path
+- native_vlan path
+
+DELETE VLAN IN USE
+- untagged use
+- tagged use
+- mixed use
+- unused VLAN delete
+- allow_vlan_delete_in_use True path
+- check mode warning
+- dual syntax conflict still fails even with allow_vlan_delete_in_use
+
+MODE NONE -> VLAN 1
+- access port becomes none -> effective VLAN 1
+- trunk port becomes none -> effective VLAN 1
+- warning present
+- diff shows VLAN 1
+- no unsupported none-mode backend error for these cases
+- fallback still respects access↔trunk protection
+
+REGRESSION
+- Step 1 semantics unchanged
+- Step 2 mode-change behavior unchanged
+- Step 3 dual syntax conflict behavior unchanged
+- None vs [] unchanged
+- all ports remain 1-based in output
+
+QUALITY
+- ruff
+- mypy on changed src files
+- git diff --check
+- targeted pytest
+- full pytest if feasible
+
+PART 11 — DOCUMENTATION
+
+Update README and/or developer docs with a short "policy" section explaining:
+
+Default behavior:
+- destructive operations fail unless explicitly allowed
+- empty membership is automatically mapped to access VLAN 1
+
+Flags:
+- allow_untagged_move
+- allow_vlan_delete_in_use
+
+Explain with examples.
+
+Add an explicit note:
+- "If a port would otherwise end up with no VLAN membership, the policy layer maps it to access VLAN 1."
+
+PART 12 — DELIVERABLE FORMAT
+
+When finished, report back with:
+
+1) Summary of Step 4 implementation
+2) Modified files
+3) Exact behavior for:
+   - untagged move
+   - delete VLAN in use
+   - mode none -> VLAN 1 mapping
+4) Exact warning/error behavior in check mode vs apply mode
+5) Test results
+6) Explicit statement:
+   "Step 4 is ready for review"
+
diff --git a/src/napalm_jtcom/driver.py b/src/napalm_jtcom/driver.py
index 60a6559..0a22af6 100644
--- a/src/napalm_jtcom/driver.py
+++ b/src/napalm_jtcom/driver.py
@@ -89,6 +89,10 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
         self._allow_port_mode_change: bool = bool(
             self.optional_args.get("allow_port_mode_change", False)
         )
+        self._allow_untagged_move: bool = bool(
+            self.optional_args.get("allow_untagged_move", False)
+        )
+        self._allow_vlan_delete_in_use: bool = bool(self.optional_args.get("allow_vlan_delete_in_use", False))
 
         logger.debug(
             "JTComDriver initialised: host=%s port=%d user=%s",
@@ -252,6 +256,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
         *,
         dry_run: bool = False,
         allow_port_mode_change: bool | None = None,
+        allow_untagged_move: bool | None = None,
+        allow_vlan_delete_in_use: bool | None = None,
     ) -> dict[str, Any]:
         """Apply an incremental VLAN change plan to the switch.
 
@@ -270,6 +276,10 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
                 applying anything to the switch.
             allow_port_mode_change: Override ``optional_args["allow_port_mode_change"]``
                 for this call.  ``False`` blocks access↔trunk transitions.
+            allow_untagged_move: Override ``optional_args["allow_untagged_move"]``.
+                ``False`` blocks untagged/native VLAN moves.
+            allow_vlan_delete_in_use: Override ``optional_args["allow_vlan_delete_in_use"]``.
+                ``True`` allows auto-detaching VLANs before deletion.
 
         Returns:
             A dict with keys:
@@ -299,6 +309,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
             desired_vlans,
             check_mode=dry_run,
             allow_port_mode_change=allow_port_mode_change,
+            allow_untagged_move=allow_untagged_move,
+            allow_vlan_delete_in_use=allow_vlan_delete_in_use,
         )
 
         result: dict[str, Any] = {
@@ -430,6 +442,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
         *,
         check_mode: bool = False,
         backup_before_change: bool | None = None,
+        allow_untagged_move: bool | None = None,
+        allow_vlan_delete_in_use: bool | None = None,
     ) -> dict[str, Any]:
         """Apply an incremental device configuration to the switch idempotently.
 
@@ -446,6 +460,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
             check_mode: If ``True``, return the plan without applying anything.
             backup_before_change: Override the ``backup_before_change`` optional
                 arg.  ``None`` means use the optional_args value (default ``True``).
+            allow_untagged_move: Override ``optional_args["allow_untagged_move"]``.
+            allow_vlan_delete_in_use: Override ``optional_args["allow_vlan_delete_in_use"]``.
 
         Returns:
             A dict with keys:
@@ -496,6 +512,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
             desired_plan_n.vlans,
             check_mode=check_mode,
             allow_port_mode_change=None,
+            allow_untagged_move=allow_untagged_move,
+            allow_vlan_delete_in_use=allow_vlan_delete_in_use,
         )
         if membership_plan.changed_ports or membership_plan.warnings:
             diff["vlan_membership"] = {
@@ -606,6 +624,8 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
         *,
         check_mode: bool,
         allow_port_mode_change: bool | None,
+        allow_untagged_move: bool | None,
+        allow_vlan_delete_in_use: bool | None,
     ) -> VlanMembershipPlan:
         """Build a VLAN membership plan from current switch state."""
         known_ports = [settings.port_id for settings in current_ports]
@@ -615,10 +635,16 @@ class JTComDriver(NetworkDriver):  # type: ignore[misc]
             if allow_port_mode_change is None
             else allow_port_mode_change
         )
+        allow_move = (
+            self._allow_untagged_move if allow_untagged_move is None else allow_untagged_move
+        )
+        force_delete = self._allow_vlan_delete_in_use if allow_vlan_delete_in_use is None else allow_vlan_delete_in_use
         return plan_vlan_membership_changes(
             current_per_port,
             desired_vlans.values(),
             allow_port_mode_change=allow,
+            allow_untagged_move=allow_move,
+            allow_vlan_delete_in_use=force_delete,
             check_mode=check_mode,
         )
 
diff --git a/src/napalm_jtcom/utils/vlan_membership.py b/src/napalm_jtcom/utils/vlan_membership.py
index 16f553a..3bdecb1 100644
--- a/src/napalm_jtcom/utils/vlan_membership.py
+++ b/src/napalm_jtcom/utils/vlan_membership.py
@@ -26,9 +26,12 @@ class PortVlanIntent(TypedDict):
     permit_vlans: list[int]
 
 _MODE_CHANGE_HINT = "Set allow_port_mode_change=true to allow this access/trunk transition."
+_UNTAGGED_MOVE_HINT = "Set allow_untagged_move=True to allow this untagged/native VLAN move."
+_VLAN_DELETE_IN_USE_HINT = (
+    "Set allow_vlan_delete_in_use=True to detach this VLAN from affected ports before deletion."
+)
 _NONE_MODE_HINT = (
-    "JTCom CGI supports only access/trunk VLAN port modes; desired mode 'none' "
-    "cannot be applied safely."
+    "Policy fallback maps ports with no VLAN membership to access VLAN 1."
 )
 
 
@@ -62,6 +65,37 @@ class VlanMembershipUnsupportedModeError(ValueError):
         super().__init__(f"Unsupported VLAN port mode requested: {details}")
 
 
+class VlanMembershipUntaggedMoveError(ValueError):
+    """Raised when an untagged/native VLAN move is blocked by policy."""
+
+    def __init__(self, warnings: list[dict[str, Any]]) -> None:
+        self.warnings = warnings
+        details = "; ".join(
+            (
+                f"port_id={w['port_id']} current_untagged_vlan={w['current_untagged_vlan']} "
+                f"desired_untagged_vlan={w['desired_untagged_vlan']}"
+            )
+            for w in warnings
+        )
+        super().__init__(f"Untagged/native VLAN move blocked: {details}. {_UNTAGGED_MOVE_HINT}")
+
+
+class VlanDeleteInUseError(ValueError):
+    """Raised when deleting a VLAN that is still referenced by ports is blocked."""
+
+    def __init__(self, warnings: list[dict[str, Any]]) -> None:
+        self.warnings = warnings
+        details = "; ".join(
+            (
+                f"vlan_id={w['vlan_id']} tagged={w['affected_ports_tagged']} "
+                f"untagged={w['affected_ports_untagged']}"
+            )
+            for w in warnings
+        )
+        super().__init__(f"VLAN delete blocked because VLAN is still in use: {details}. "
+                         f"{_VLAN_DELETE_IN_USE_HINT}")
+
+
 @dataclass
 class VlanMembershipPlan:
     """Computed VLAN membership plan."""
@@ -134,6 +168,8 @@ def plan_vlan_membership_changes(
     vlan_configs: Iterable[VlanConfig],
     *,
     allow_port_mode_change: bool = False,
+    allow_untagged_move: bool = False,
+    allow_vlan_delete_in_use: bool = False,
     check_mode: bool = False,
 ) -> VlanMembershipPlan:
     """Apply VLAN membership operations in memory and produce a device intent.
@@ -144,18 +180,37 @@ def plan_vlan_membership_changes(
     """
     current = copy_membership_map(current_per_port)
     desired = copy_membership_map(current_per_port)
+    configs = sorted(vlan_configs, key=lambda item: item.vlan_id)
 
-    for cfg in sorted(vlan_configs, key=lambda item: item.vlan_id):
+    for cfg in configs:
         if cfg.state != "present":
             continue
         _apply_vlan_config(desired, cfg)
 
     normalize_tagged_untagged_consistency(desired)
+    delete_in_use_warnings = detect_vlan_delete_in_use_warnings(desired, configs)
+    if delete_in_use_warnings and not allow_vlan_delete_in_use and not check_mode:
+        raise VlanDeleteInUseError(delete_in_use_warnings)
+    if allow_vlan_delete_in_use:
+        for warning in delete_in_use_warnings:
+            _detach_vlan(desired, int(warning["vlan_id"]))
+
+    untagged_move_warnings = detect_untagged_move_warnings(current, desired)
+    if untagged_move_warnings and not allow_untagged_move and not check_mode:
+        raise VlanMembershipUntaggedMoveError(untagged_move_warnings)
+
+    mode_none_warnings = apply_mode_none_fallback(desired, changed_ports(current, desired))
     desired_port_vlan = build_desired_port_vlan(desired)
     changed = changed_ports(current, desired)
     mode_change_warnings = detect_mode_change_warnings(current, desired)
     unsupported_mode_warnings = detect_unsupported_mode_warnings(desired_port_vlan, changed)
-    warnings = mode_change_warnings + unsupported_mode_warnings
+    warnings = (
+        delete_in_use_warnings
+        + untagged_move_warnings
+        + mode_none_warnings
+        + mode_change_warnings
+        + unsupported_mode_warnings
+    )
 
     if unsupported_mode_warnings and not check_mode:
         raise VlanMembershipUnsupportedModeError(unsupported_mode_warnings)
@@ -220,6 +275,81 @@ def detect_mode_change_warnings(
     return warnings
 
 
+def detect_untagged_move_warnings(
+    current_per_port: PortMembershipMap,
+    desired_per_port: PortMembershipMap,
+) -> list[dict[str, Any]]:
+    """Return untagged/native VLAN moves that require explicit policy override."""
+    warnings: list[dict[str, Any]] = []
+    for port_id in sorted(set(current_per_port) | set(desired_per_port)):
+        current_vlan = _untagged_vlan(current_per_port.get(port_id, make_port_state()))
+        desired_vlan = _untagged_vlan(desired_per_port.get(port_id, make_port_state()))
+        if current_vlan is not None and desired_vlan is not None and current_vlan != desired_vlan:
+            warnings.append(
+                {
+                    "type": "untagged_move",
+                    "port_id": port_id,
+                    "current_untagged_vlan": current_vlan,
+                    "desired_untagged_vlan": desired_vlan,
+                    "hint": _UNTAGGED_MOVE_HINT,
+                }
+            )
+    return warnings
+
+
+def detect_vlan_delete_in_use_warnings(
+    desired_per_port: PortMembershipMap,
+    vlan_configs: Iterable[VlanConfig],
+) -> list[dict[str, Any]]:
+    """Return absent VLANs still referenced in the effective desired state."""
+    absent_vlans = sorted({cfg.vlan_id for cfg in vlan_configs if cfg.state == "absent"})
+    warnings: list[dict[str, Any]] = []
+    for vlan_id in absent_vlans:
+        tagged_ports: list[int] = []
+        untagged_ports: list[int] = []
+        for port_id, state in sorted(desired_per_port.items()):
+            if vlan_id in _tagged_vlans(state):
+                tagged_ports.append(port_id)
+            if _untagged_vlan(state) == vlan_id:
+                untagged_ports.append(port_id)
+        if tagged_ports or untagged_ports:
+            warnings.append(
+                {
+                    "type": "vlan_delete_in_use",
+                    "vlan_id": vlan_id,
+                    "affected_ports_tagged": tagged_ports,
+                    "affected_ports_untagged": untagged_ports,
+                    "hint": _VLAN_DELETE_IN_USE_HINT,
+                }
+            )
+    return warnings
+
+
+def apply_mode_none_fallback(
+    desired_per_port: PortMembershipMap,
+    candidate_port_ids: Iterable[int],
+) -> list[dict[str, Any]]:
+    """Map desired mode ``none`` ports to access VLAN 1 and return warnings."""
+    warnings: list[dict[str, Any]] = []
+    for port_id in sorted(set(candidate_port_ids)):
+        state = desired_per_port.get(port_id, make_port_state())
+        if classify_port_mode(state) != "none":
+            continue
+        desired_per_port[port_id] = state
+        state["untagged_vlan"] = 1
+        _tagged_vlans(state).clear()
+        warnings.append(
+            {
+                "type": "mode_none_mapped_to_vlan1",
+                "port_id": port_id,
+                "mapped_vlan": 1,
+                "reason": "policy_default_vlan1_fallback",
+                "hint": _NONE_MODE_HINT,
+            }
+        )
+    return warnings
+
+
 def detect_unsupported_mode_warnings(
     desired_port_vlan: dict[int, PortVlanIntent],
     changed_port_ids: Iterable[int],
@@ -244,6 +374,13 @@ def detect_unsupported_mode_warnings(
     return warnings
 
 
+def _detach_vlan(per_port: PortMembershipMap, vlan_id: int) -> None:
+    for state in per_port.values():
+        _tagged_vlans(state).discard(vlan_id)
+        if _untagged_vlan(state) == vlan_id:
+            state["untagged_vlan"] = None
+
+
 def build_desired_port_vlan(per_port: PortMembershipMap) -> dict[int, PortVlanIntent]:
     """Build full device-level per-port VLAN intent."""
     intent: dict[int, PortVlanIntent] = {}
diff --git a/tests/unit/test_port_vlan_input.py b/tests/unit/test_port_vlan_input.py
index 6213c74..4b86e03 100644
--- a/tests/unit/test_port_vlan_input.py
+++ b/tests/unit/test_port_vlan_input.py
@@ -225,3 +225,110 @@ def test_driver_dual_syntax_conflict_fails_before_mutation(
 
     session.post.assert_not_called()
     session.download_config_backup.assert_not_called()
+
+
+def test_port_centric_untagged_move_fails_by_default(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    driver = JTComDriver("192.0.2.1", "admin", "admin")
+    driver._session = MagicMock()
+    current_vlans = {
+        20: VlanEntry(vlan_id=20, name="v20", untagged_ports=["Port 5"]),
+        30: VlanEntry(vlan_id=30, name="v30"),
+    }
+    current_ports = [PortSettings(port_id=5, name="Port 5", admin_up=True)]
+    monkeypatch.setattr(
+        driver,
+        "_read_current_state",
+        lambda _session: (current_vlans, current_ports),
+    )
+
+    with pytest.raises(ValueError, match="Untagged/native VLAN move blocked"):
+        driver.apply_device_config(
+            DeviceConfig(ports={5: PortConfig(port_id=5, access_vlan=30)})
+        )
+
+
+def test_port_centric_untagged_move_allowed_with_flag(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    driver = JTComDriver(
+        "192.0.2.1",
+        "admin",
+        "admin",
+        optional_args={"allow_untagged_move": True},
+    )
+    driver._session = MagicMock()
+    current_vlans = {
+        20: VlanEntry(vlan_id=20, name="v20", untagged_ports=["Port 5"]),
+        30: VlanEntry(vlan_id=30, name="v30"),
+    }
+    current_ports = [PortSettings(port_id=5, name="Port 5", admin_up=True)]
+    monkeypatch.setattr(
+        driver,
+        "_read_current_state",
+        lambda _session: (current_vlans, current_ports),
+    )
+
+    result = driver.apply_device_config(
+        DeviceConfig(ports={5: PortConfig(port_id=5, access_vlan=30)}),
+        check_mode=True,
+    )
+
+    assert result["changed_ports"] == [5]
+    assert result["warnings"][0]["type"] == "untagged_move"
+    assert result["after"][5]["untagged_vlan"] == 30
+
+
+def test_allow_vlan_delete_in_use_does_not_suppress_dual_syntax_conflict(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    driver = JTComDriver(
+        "192.0.2.1",
+        "admin",
+        "admin",
+        optional_args={"allow_vlan_delete_in_use": True},
+    )
+    driver._session = MagicMock()
+    current_vlans = {20: VlanEntry(vlan_id=20, name="v20")}
+    current_ports = [PortSettings(port_id=5, name="Port 5", admin_up=True)]
+    monkeypatch.setattr(
+        driver,
+        "_read_current_state",
+        lambda _session: (current_vlans, current_ports),
+    )
+
+    with pytest.raises(DualSyntaxConflictError):
+        driver.apply_device_config(
+            DeviceConfig(
+                vlans={20: VlanConfig(vlan_id=20, state="absent")},
+                ports={5: PortConfig(port_id=5, trunk_add_vlans=[20])},
+            )
+        )
+
+
+def test_allow_untagged_move_does_not_suppress_dual_syntax_conflict(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    driver = JTComDriver(
+        "192.0.2.1",
+        "admin",
+        "admin",
+        optional_args={"allow_untagged_move": True},
+    )
+    driver._session = MagicMock()
+    current_vlans = {10: VlanEntry(vlan_id=10, name="v10"), 20: VlanEntry(vlan_id=20, name="v20")}
+    current_ports = [PortSettings(port_id=5, name="Port 5", admin_up=True)]
+    monkeypatch.setattr(
+        driver,
+        "_read_current_state",
+        lambda _session: (current_vlans, current_ports),
+    )
+
+    with pytest.raises(DualSyntaxConflictError):
+        driver.apply_device_config(
+            DeviceConfig(
+                vlans={10: VlanConfig(vlan_id=10, untagged_add=[5])},
+                ports={5: PortConfig(port_id=5, access_vlan=20)},
+            )
+        )
diff --git a/tests/unit/test_vlan_membership_apply.py b/tests/unit/test_vlan_membership_apply.py
index 1c5299a..99597fb 100644
--- a/tests/unit/test_vlan_membership_apply.py
+++ b/tests/unit/test_vlan_membership_apply.py
@@ -14,9 +14,10 @@ from napalm_jtcom.driver import JTComDriver
 from napalm_jtcom.model.port import PortSettings
 from napalm_jtcom.model.vlan import VlanConfig, VlanEntry
 from napalm_jtcom.utils.vlan_membership import (
+    VlanDeleteInUseError,
     VlanMembershipModeChangeError,
     VlanMembershipPlan,
-    VlanMembershipUnsupportedModeError,
+    VlanMembershipUntaggedMoveError,
     build_current_per_port_from_vlans,
     build_desired_port_vlan,
     make_port_state,
@@ -52,7 +53,11 @@ def test_tagged_set_replaces_vlan_dimension_only() -> None:
 
 def test_untagged_add_assigns_native_vlan() -> None:
     current = {3: make_port_state(untagged_vlan=10)}
-    plan = plan_vlan_membership_changes(current, [VlanConfig(vlan_id=20, untagged_add=[3])])
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=20, untagged_add=[3])],
+        allow_untagged_move=True,
+    )
     assert plan.desired_per_port[3]["untagged_vlan"] == 20
 
 
@@ -63,7 +68,8 @@ def test_untagged_remove_clears_native_if_matching() -> None:
         [VlanConfig(vlan_id=20, untagged_remove=[3])],
         check_mode=True,
     )
-    assert plan.desired_per_port[3]["untagged_vlan"] is None
+    assert plan.desired_per_port[3]["untagged_vlan"] == 1
+    assert plan.warnings[0]["type"] == "mode_none_mapped_to_vlan1"
 
 
 def test_tagged_untagged_same_vlan_is_auto_normalized() -> None:
@@ -185,21 +191,24 @@ def test_check_mode_warns_instead_of_failing_on_desired_mode_none() -> None:
         [VlanConfig(vlan_id=20, untagged_remove=[3])],
         check_mode=True,
     )
-    assert plan.desired_port_vlan[3]["mode"] == "none"
-    assert plan.warnings[0]["type"] == "unsupported_vlan_port_mode"
+    assert plan.desired_port_vlan[3]["mode"] == "access"
+    assert plan.desired_per_port[3]["untagged_vlan"] == 1
+    assert plan.warnings[0]["type"] == "mode_none_mapped_to_vlan1"
     assert plan.warnings[0]["port_id"] == 3
-    assert "access/trunk" in plan.warnings[0]["hint"]
+    assert plan.warnings[0]["mapped_vlan"] == 1
 
 
-def test_apply_mode_fails_on_desired_mode_none() -> None:
+def test_apply_mode_maps_desired_mode_none_to_vlan1() -> None:
     current = {3: make_port_state(untagged_vlan=20)}
-    with pytest.raises(VlanMembershipUnsupportedModeError) as exc_info:
-        plan_vlan_membership_changes(current, [VlanConfig(vlan_id=20, untagged_remove=[3])])
-    assert exc_info.value.warnings[0]["port_id"] == 3
-    assert "cannot be applied safely" in str(exc_info.value)
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=20, untagged_remove=[3])],
+    )
+    assert plan.desired_port_vlan[3] == {"mode": "access", "native_vlan": 1, "permit_vlans": [1]}
+    assert plan.warnings[0]["type"] == "mode_none_mapped_to_vlan1"
 
 
-def test_set_vlans_mode_none_failure_happens_before_device_mutation(
+def test_set_vlans_mode_none_maps_to_vlan1_before_apply(
     monkeypatch: pytest.MonkeyPatch,
 ) -> None:
     driver = JTComDriver("192.0.2.1", "admin", "admin")
@@ -217,13 +226,107 @@ def test_set_vlans_mode_none_failure_happens_before_device_mutation(
         lambda _session: (current_vlans, current_ports),
     )
 
-    with pytest.raises(VlanMembershipUnsupportedModeError):
-        driver.set_vlans({20: VlanConfig(vlan_id=20, untagged_remove=[4])})
+    result = driver.set_vlans(
+        {20: VlanConfig(vlan_id=20, untagged_remove=[4])},
+        dry_run=True,
+    )
 
-    session.post.assert_not_called()
+    assert result["after"][4] == {"untagged_vlan": 1, "tagged_vlans": []}
+    assert result["warnings"][0]["type"] == "mode_none_mapped_to_vlan1"
     session.download_config_backup.assert_not_called()
 
 
+def test_untagged_move_apply_fails_by_default() -> None:
+    current = {3: make_port_state(untagged_vlan=20)}
+    with pytest.raises(VlanMembershipUntaggedMoveError) as exc_info:
+        plan_vlan_membership_changes(current, [VlanConfig(vlan_id=30, untagged_add=[3])])
+    assert exc_info.value.warnings[0]["type"] == "untagged_move"
+    assert exc_info.value.warnings[0]["current_untagged_vlan"] == 20
+    assert exc_info.value.warnings[0]["desired_untagged_vlan"] == 30
+
+
+def test_untagged_move_check_mode_warns() -> None:
+    current = {3: make_port_state(untagged_vlan=20)}
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=30, untagged_add=[3])],
+        check_mode=True,
+    )
+    assert plan.warnings[0]["type"] == "untagged_move"
+    assert plan.desired_per_port[3]["untagged_vlan"] == 30
+
+
+def test_untagged_move_allowed_with_flag() -> None:
+    current = {3: make_port_state(untagged_vlan=20)}
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=30, untagged_add=[3])],
+        allow_untagged_move=True,
+    )
+    assert plan.desired_per_port[3]["untagged_vlan"] == 30
+    assert plan.warnings[0]["type"] == "untagged_move"
+
+
+def test_native_vlan_move_on_trunk_fails_by_default() -> None:
+    current = {5: make_port_state(untagged_vlan=10, tagged_vlans={20})}
+    with pytest.raises(VlanMembershipUntaggedMoveError):
+        plan_vlan_membership_changes(current, [VlanConfig(vlan_id=30, untagged_add=[5])])
+
+
+def test_delete_vlan_in_use_fails_by_default() -> None:
+    current = {3: make_port_state(untagged_vlan=20)}
+    with pytest.raises(VlanDeleteInUseError) as exc_info:
+        plan_vlan_membership_changes(current, [VlanConfig(vlan_id=20, state="absent")])
+    assert exc_info.value.warnings[0]["type"] == "vlan_delete_in_use"
+    assert exc_info.value.warnings[0]["affected_ports_untagged"] == [3]
+
+
+def test_delete_vlan_in_use_check_mode_warns() -> None:
+    current = {3: make_port_state(untagged_vlan=20)}
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=20, state="absent")],
+        check_mode=True,
+    )
+    assert plan.warnings[0]["type"] == "vlan_delete_in_use"
+    assert plan.changed_ports == []
+
+
+def test_allow_vlan_delete_in_use_detaches_and_falls_back_to_vlan1() -> None:
+    current = {
+        3: make_port_state(untagged_vlan=20),
+        5: make_port_state(untagged_vlan=10, tagged_vlans={20, 30}),
+    }
+    plan = plan_vlan_membership_changes(
+        current,
+        [VlanConfig(vlan_id=20, state="absent")],
+        allow_vlan_delete_in_use=True,
+        check_mode=True,
+    )
+    assert plan.desired_per_port[3]["untagged_vlan"] == 1
+    assert plan.desired_per_port[5]["untagged_vlan"] == 10
+    assert plan.desired_per_port[5]["tagged_vlans"] == {30}
+    assert [warning["type"] for warning in plan.warnings][:2] == [
+        "vlan_delete_in_use",
+        "mode_none_mapped_to_vlan1",
+    ]
+
+
+def test_delete_unused_vlan_is_allowed_without_warning() -> None:
+    current = {3: make_port_state(untagged_vlan=10)}
+    plan = plan_vlan_membership_changes(current, [VlanConfig(vlan_id=20, state="absent")])
+    assert plan.warnings == []
+    assert plan.changed_ports == []
+
+
+def test_mode_none_fallback_still_respects_trunk_to_access_protection() -> None:
+    current = {5: make_port_state(tagged_vlans={20})}
+    with pytest.raises(VlanMembershipModeChangeError) as exc_info:
+        plan_vlan_membership_changes(current, [VlanConfig(vlan_id=20, tagged_remove=[5])])
+    assert exc_info.value.warnings[0]["current_mode"] == "trunk"
+    assert exc_info.value.warnings[0]["desired_mode"] == "access"
+
+
 @pytest.mark.parametrize(
     "path",
     [
