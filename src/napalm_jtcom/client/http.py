"""Low-level HTTP helpers and request builders for JTCom CGI endpoints."""

from __future__ import annotations

# HTTP utility functions and constants will be added here as the driver
# implementation progresses. This module keeps raw HTTP concerns separate
# from session lifecycle (session.py) and business logic (driver.py).
